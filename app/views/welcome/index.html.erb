<h1>This is the Divx welcome page</h1>
<div class="container">
  <% if !user_signed_in? %>
  <div class="row">
    <button type="button" class="btn btn-info"><%= link_to "Talent Sign Up", new_user_registration_path %></button>
  </div>
  <% end %>
</div>


<% if user_signed_in? %>
  <%= link_to "Log Out", destroy_user_session_path, method: :delete %>
<% end %>
